import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.todayapp.com',
  appName: 'today',
  webDir: 'dist/today-ui',
  bundledWebRuntime: false,
};

export default config;
